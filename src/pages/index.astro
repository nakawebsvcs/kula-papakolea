---
import BaseLayout from "@layouts/BaseLayout.astro";
import CTA from "@components/CTA.astro";
import FAQ from "@components/FAQ.astro";

// Import the hero video
import heroVideo from "@assets/videos/Aerials-Combined.mp4";

// Optimize our landing image and pass it as props to the BaseLayout (for preloading) and Landing (for rendering)
import landingImage from "@assets/images/landing.jpg" // <-- THE PATH TO THE ASSET YOU WANT TO PRELOAD - The asset must live in src
import { getImage } from "astro:assets";
const optimizedImage = await getImage({src: landingImage, format: 'avif'})
---

<BaseLayout
  title="Kula No Nā Poʻe Hawaiʻi"
  description="Serving Papakolea Homesteads for over 30 Years"
  preloadedImage = {optimizedImage}
>
  <!-- ============================================ -->
  <!--                    Hero                      -->
  <!-- ============================================ -->

 <section id="hero" class="hero">
  <video
    class="hero-video"
    autoplay
    muted
    loop
    playsinline
  >
    <source src={heroVideo} type="video/mp4" />
    Your browser does not support the video tag.
  </video>
</section>

<!-- ============================================ -->
<!--                  Content                     -->
<!-- ============================================ -->

<section id="content-1450">
    <div class="cs-container">
        <div class="cs-content">
            <img class="cs-topper" src="/assets/images/logo-small.png" style="width: 15rem;"/>
            <h2 class="cs-title playfair-display">Kula No Nā Poʻe Hawaiʻi</h2>
            <p class="cs-text">
                Serving Papakolea Homesteads for over 30 Years
            </p>
            <p class="cs-title subhead">
                Aloha mai kākou!
            </p>
        </div>
    </div>
</section>
                                

 <!-- ============================================ -->
<!--                   Services                   -->
<!-- ============================================ -->

<section id="services-38">
    <div class="cs-container">
        <ul class="cs-card-group">
            <li class="cs-item photo-1">
                <div class="cs-picture">
                    <picture>
                        <source media="(max-width: 600px)" srcset="/assets/images/Community-Led-Initiatives.jpg">
                        <source media="(min-width: 601px)" srcset="/assets/images/Community-Led-Initiatives.jpg">
                        <img loading="lazy" decoding="async" src="/assets/images/Community-Led-Initiatives.jpg" alt="community-led initiatives" width="348" height="433" aria-hidden="true">
                    </picture>
                </div>
                <div class="cs-box">
                    <h3 class="cs-h3">Community-Led <span>Initiatives</span></h3>
                    <p class="cs-item-text">
                        We provide dynamic and responsive education, health, and social service programs that support the long term health and well-being of families in Papakōlea, Kewalo, and Kalawahine by addressing the social determinants of health.
                    </p>
                </div>
            </li>

            <li class="cs-item photo-2">
                <div class="cs-picture">
                    <picture>
                        <source media="(max-width: 600px)" srcset="/assets/images/Community-Engagement.jpg">
                        <source media="(min-width: 601px)" srcset="/assets/images/Community-Engagement.jpg">
                        <img loading="lazy" decoding="async" src="/assets/images/Community-Engagement.jpg" alt="community engagement" width="348" height="433" aria-hidden="true">
                    </picture>
                </div>
                <div class="cs-box">
                    <h3 class="cs-h3">Community <span>Engagement</span></h3>
                    <p class="cs-item-text">
                        We support annual community events that nurture Papakōlea’s strong sense of community pride, unity, and resilience, while encouraging participation in programs and research with the protective tenets of Cultural Safety and Community Based Participatory Research.
                    </p>
                </div>
            </li>

            <li class="cs-item photo-3">
                <div class="cs-picture">
                    <picture>
                        <source media="(max-width: 600px)" srcset="/assets/images/Building-Community-Capacity.JPEG">
                        <source media="(min-width: 601px)" srcset="/assets/images/Building-Community-Capacity.JPEG">
                        <img loading="lazy" decoding="async" src="/assets/images/Building-Community-Capacity.JPEG" alt="building community capacity" width="348" height="433" aria-hidden="true">
                    </picture>
                </div>
                <div class="cs-box">
                    <h3 class="cs-h3">Building Community Capacity <span>& Lasting Partner Relationships</span></h3>
                    <p class="cs-item-text">
                        We excel at finding the right partners and maintaining positive partner relationships to help us achieve our mission, build community capacity, and ensure true reciprocal collaboration.
                    </p>
                </div>
            </li>
        </ul>
    </div>
</section>
                                

<!-- ============================================ -->
<!--                    Mission                    -->
<!-- ============================================ -->

<section id="quote-1307">
    <div class="cs-container">
        <div class="cs-content">
            <span class="cs-name">Ka Nuʻukia - Our Mission</span>
            <p class="cs-quote">
                Founded in 1992, Kula no nā Po'e Hawai'i (KULA) is a Native Hawaiian beneficiary serving organization that exists to promote cultural, educational, environmental, and health equity for all. Well-being our priority, education our commitment, community our drive.<br><br>

KULA provides the Papakōlea Hawaiian Homestead Region a multitude of programs and activities from the Papakōlea Community Center.
            </p>
            
      <!--      <span class="cs-job">Creative Director</span> -->
        </div>
    </div>
    <!--Background Image-->
    <picture class="cs-background">
        <source media="(max-width: 600px)"
                srcset="/assets/images/lai.png">
        <source media="(min-width: 601px)"
                srcset="/assets/images/lai.png">
        <img aria-hidden="true" loading="lazy" decoding="async"
                src="/assets/images/lai.png"
                alt="Ka Nuʻukia - Our Mission" width="360" height="406">
    </picture>
</section>
                                

<!-- ============================================ -->
<!--                   Featured Pages              -->
<!-- ============================================ -->

<section id="pricing-1843">
    <div class="cs-container">
        <div class="cs-content">
 <!--           <span class="cs-topper">Our Pricing</span>
            <h2 class="cs-title">Pricing Plans for Our Services</h2> -->
        </div>
        <ul class="cs-card-group">
            <li class="cs-item">
                <div class="cs-price-box">
                    <span class="cs-price">Our Work</span>
                    <img class="cs-leaf" src="/assets/images/fern-accent.png" alt="fern image" loading="lazy" decoding="async" height="175" width="142">
                </div>
                <ul class="cs-ul">
                    <li class="cs-li">
                        <img class="cs-icon" aria-hidden="true" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/dark-grey-tick.svg" alt="checkmark" width="24" height="24">
                        At vero eos et accusamus et iusto odio dignissimos ducimus qui blanditiis praesentium voluptatum deleniti atque corrupti quos.
                    </li>
                    <li class="cs-li">
                        <img class="cs-icon" aria-hidden="true" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/dark-grey-tick.svg" alt="checkmark" width="24" height="24">
                        Temporibus autem quibusdam et aut officiis debitis aut rum necessitatibus saepe eveniet ut et voluptates repudiandae sint.
                    </li>
                </ul>
                <a href="" class="cs-button-solid">Visit Page</a>
            </li>
            <li class="cs-item">
                <div class="cs-price-box cs-vip">
                    <span class="cs-price">Pilina<br>Pūowaina</span>
                    <img class="cs-leaf" src="/assets/images/fern-accent.png" alt="fern image" loading="lazy" decoding="async" height="175" width="142">
                </div>
                <ul class="cs-ul">
                    <li class="cs-li">
                        <img class="cs-icon" aria-hidden="true" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/dark-grey-tick.svg" alt="checkmark" width="24" height="24">
                        At vero eos et accusamus et iusto odio dignissimos ducimus qui blanditiis praesentium voluptatum deleniti atque corrupti quos.
                    </li>
                    <li class="cs-li">
                        <img class="cs-icon" aria-hidden="true" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Icons/dark-grey-tick.svg" alt="checkmark" width="24" height="24">
                        Temporibus autem quibusdam et aut officiis debitis aut rum necessitatibus saepe eveniet ut et voluptates repudiandae sint.
                    </li>
                </ul>
                <a href="" class="cs-button-solid">Visit Page</a>
            </li>
        </ul>
    </div>
</section>
                                
  
 <!-- ============================================ -->
<!--                   Subscribe                  -->
<!-- ============================================ -->

<section id="subscribe-2143">
    <div class="cs-container">
        <div class="cs-image-group">
            <!--Main Image-->
            <picture class="cs-picture">
                <!--Mobile Image-->
                <source media="(max-width: 600px)" srcset="/assets/images/Kolea-1.png">
                <!--Tablet and above Image-->
                <source media="(min-width: 601px)" srcset="/assets/images/Kolea-1.png">
                <img loading="lazy" decoding="async" src="/assets/images/Kolea-1.png" alt="Kōlea" width="396" height="570">
            </picture>
            <!--Pua Melia-->
            <img class="cs-wheel" loading="lazy" decoding="async" src="/assets/images/Pua-Melia-2.png" alt="Pua Melia" width="542" height="542" aria-hidden="true">
        </div>
        <div class="cs-content">
            <span class="cs-topper">Stay in Touch</span>
            <h2 class="cs-title">Join our Mailing List!</h2>
            <p class="cs-text">
                Temporibus autem quibusdam et aut officiis debitis aut rum necessitatibus saepe eveniet ut et voluptates repudiandae sint.
            </p>
            <form class="cs-form" name="Contact Form" method="post">
                <input class="cs-input" type="email" id="cs-email-2143" name="find-us" placeholder="Enter your email" required>
                <button class="cs-button-solid cs-submit" type="submit">Subscribe</button>
            </form>
        </div>
    </div>
    <!--Flower
    <img class="cs-flower" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/Graphics/wedding-flower3.png" alt="flower" width="284" height="583" aria-hidden="true"> -->
</section>
                                

</BaseLayout>

<style lang="less">
  /* STYLES FOR ABOVE THE FOLD */

  /*-- -------------------------- -->
<---           Hero             -->
<--- -------------------------- -*/

 /* filepath: /Users/jessinaka/Desktop/Coding Projects/kula-papakolea/src/pages/index.astro */
.hero {
  position: relative;
  width: 100%;
  height: 100vh; // Full screen height
  overflow: hidden;

  .hero-video {
    width: 100%;
    height: 100%;
    object-fit: cover; // Ensures the video covers the entire section
    position: absolute;
    top: 0;
    left: 0;
    z-index: 1;
  }

}

/*-- -------------------------- -->
<---          Content           -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    #content-1450 {
        padding: var(--sectionPadding);
        
        .cs-container {
            width: 100%;
            max-width: (1280/16rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            /* 48px - 64px */
            gap: clamp(3rem, 6vw, 4rem);
        }

        .cs-content {
            /* set text align to left if content needs to be left aligned */
            text-align: center;
            width: 100%;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: center;
        }

        .cs-title, .cs-text {
            max-width: 100%;
        }

        .playfair-display {
		font-family: "Playfair Display", Times, serif;
    font-size: clamp(3rem, 5vw, 4.5rem);
    text-transform: uppercase;
    font-weight: light;
    line-height: 1.2em;
    color: var(--primary);
    letter-spacing: 4px;
	}

        .cs-text {
            /* 16px - 20px */
            font-size: clamp(1rem, 2vw, 1.25rem);
            margin: 0 0 (32/16rem) 0;
            color: var(--secondary);
            font-style: italic;
        }

        .subhead {
          font-family: "Playfair Display", Times, serif;
    font-size: clamp(1.5rem, 3vw, 3.5rem);
    font-weight: light;
    line-height: 1.2em;
    color: var(--primary);
    letter-spacing: 4px;
        }

        .cs-button-solid {
            font-size: (16/16rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: (150/16rem);
            padding: 0 (48/16rem);
            background-color: var(--primary);
            border-radius: (8/16rem);
            overflow: hidden;
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }
    }
}

  /* PAGE-SPECIFIC STYLES FOR THE HOME PAGE */

/*-- -------------------------- -->
<---          Services          -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (max-width: 768px) {
    #services-38 .cs-card-group {
        background: none; /* No background on mobile */
    }
}

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    #services-38 {
        padding: var(--sectionPadding);

        .cs-container {
            width: 100%;
            max-width: (550/16rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: clamp(3rem, 6vw, 4rem);
        }

        .cs-card-group {
            width: 100%;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            column-gap: (20/16rem);
            row-gap: (60/16rem);
        }

        .cs-item {
            list-style: none;
            width: 100%;
            margin: 0;

            &:hover {
                .cs-picture {
                    &:before {
                        height: 100%;
                    }

                    img {
                        transform: scale(1.1);
                    }
                }
            }
        }

        .cs-picture {
            position: relative;
            display: block;
            overflow: visible;
            z-index: 2;
            aspect-ratio: 328 / 360;

            img {
                position: absolute;
                top: 0;
                left: 0;
                height: 100%;
                width: 100%;
                object-fit: cover;
                border-radius: (10/16rem);
                transition: transform 0.7s;
            }

            &:before {
                content: "";
                position: absolute;
                display: block;
                height: 0%;
                width: 100%;
                background: var(--primaryLight);
                opacity: 0.8;
                top: 0;
                left: 0;
                z-index: 1;
                transition: height 0.3s;
            }
        }

        .cs-box {
            text-align: left;
            width: 90%;
            margin: 0 auto;
            margin-top: clamp(1.5rem, 2vw, 3rem);
            padding: (24/16rem) (16/16rem) 0;
            background: #fff;
            position: relative;
            z-index: 10;
            box-sizing: border-box;
        }

        .cs-h3 {
            font-size: clamp(1.25rem, 2vw, 1.5625rem);
            text-align: center;
            line-height: 1.2em;
            font-weight: bold;
            color: var(--secondary);
            margin: 0 0 (16/16rem);
        }

        .cs-item-text {
            font-size: (20/16rem);
            line-height: 1.5em;
            margin: 0 0 (4/16rem);
            padding: 0;
            color: var(--bodyTextColor);
        }

        .cs-link {
            font-size: (16/16rem);
            line-height: clamp(2.875em, 5.5vw, 3.5em);
            text-decoration: none;
            font-weight: 700;
            margin: auto;
            color: var(--primary);
            background-color: transparent;
            border-radius: (4/16rem);
            display: inline-block;
            position: relative;
            z-index: 1;

            &:before {
                content: "";
                position: absolute;
                display: block;
                height: (3/16rem);
                width: 0%;
                background: var(--primary);
                opacity: 1;
                bottom: (6/16rem);
                left: 0;
                z-index: -1;
                border-radius: (4/16rem);
                transition: width 0.3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #services-38 {
        .cs-container {
            max-width: (1280/16rem);
        }

        .cs-card-group {
            flex-direction: row;
            align-items: stretch;
        }

        .cs-item {
            margin: 0;
        }

        .cs-picture {
            height: clamp(22.5rem, 42vw, 26.875rem);
            aspect-ratio: initial;
            position: relative;
        }

        .cs-box {
            width: 90%;
            padding: (24/16rem) (12/16rem);
        }

        .cs-h3 {
            span {
                display: block;
            }
        }
    }
}

/* Lauhala Accent Background */

/* Tablet and Desktop - 769px and up */
@media only screen and (min-width: 769px) {
    #services-38 .cs-card-group {
        position: relative; /* Ensure the background sits behind the images */
        padding: 2rem; /* Add padding around the content */
    }

    #services-38 .cs-card-group::before {
        content: "";
        position: absolute;
        top: -6rem; /* Start the background 4rem above the images */
        left: 4;
        width: 112%;
        height: calc(60% - 4rem); /* Adjust height to stop before the text */
        background: url('/assets/images/lauhala-bg.png') repeat center / contain; /* Add a repeating background */
        background-size: calc(20% - 2px) auto;
        opacity: 0.7;
        z-index: 1; /* Ensure it sits behind the images */
    }

    #services-38 .cs-item {
        position: relative; /* Ensure images are positioned above the background */
        z-index: 2; /* Bring the images above the background */
    }

    #services-38 .cs-box {
        position: relative;
        z-index: 3; /* Ensure the text content is above the background */
        background: #fff; /* Optional: Add a white background to the text box */
    }
}

/*-- -------------------------- -->
<---         Mission           -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    #quote-1307 {
        position: relative;
        min-height: 44vw;
        overflow: visible;
        z-index: 1;

        .cs-container {
            width: 100%;
            max-width: calc(1280 / 16rem);
            margin: auto;
            box-sizing: border-box;
            padding: var(--sectionPadding);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        .cs-content {
            width: 100%;
            max-width: calc(1100 / 16rem);
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: left;
        }

        .cs-quote {
            font-size: clamp(1.2rem, 1.9vw, 1.8rem);
            letter-spacing: 2px;
            line-height: 1.3em;
            font-weight: normal;
            margin-top: 2rem;
            margin-bottom: calc(24 / 16rem);
            color: var(--bodyTextColor);
            text-align: inherit;
        }

        .cs-name {
            font-family: 'Playfair Display', Times, serif;
            font-size: clamp(1.25rem, 3.25vw, 1.5625rem);
            line-height: 1.2em;
            font-weight: 700;
            margin: 0;
            margin-bottom: calc(8 / 16rem);
            color: var(--bodyTextColor);
            display: block;
            text-align: left;
        }

        .cs-job {
            font-size: calc(16 / 16rem);
            line-height: 1.5em;
            margin: 0;
            color: #f7f7f7;
            text-align: inherit;
        }

        .cs-background {
            width: 100%;
            height: 100%;
            background-color: #ffffff;
            position: absolute;
            top: 0;
            left: 0;
            z-index: -1;

            &::before {
                content: "";
                width: 100%;
                height: 100%;
                position: absolute;
                z-index: 1;
            }

            img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                opacity: 0.4;
                position: absolute;
                top: 0;
                left: 0;
            }
        }

        &::after {
            content: "";
            position: absolute;
            bottom: -1rem;
            left: 0;
            width: 100%;
            height: 13rem;
            background: url('/assets/images/fern-divider.png') repeat-x center / contain;
            background-position: 0;
            background-size: calc(60% - 2px) auto;
            z-index: 2;
            pointer-events: none;
        }
    }
}

/* Tablet styles (under 1024px) */
@media only screen and (max-width: 1024px) {
    #quote-1307 {
        position: relative;
        overflow: hidden; /* Prevents the ::after element from spilling out */
        z-index: 1;

        .cs-container {
            padding-bottom: 12rem; /* Add padding to prevent overlap with the next section */
        }

        .cs-content {
            position: relative;
            z-index: 2; /* Ensure text content is above the ::after element */
        }

        &::after {
        content: "";
        position: absolute;
        bottom: 0; /* Anchor to the bottom of the section */
        left: 0;
        width: 100%;
        height: 10rem; /* Adjust height for tablet */
        background: url('/assets/images/fern-divider.png') repeat-x center / contain; /* Repeat horizontally */
        background-position: bottom center; /* Keep it aligned to the bottom */
        z-index: 1; /* Ensure it appears above the background but below the text */
        pointer-events: none; /* Prevent interaction with the overlay */
        }
    }
}

/* Mobile styles (under 768px) */
@media only screen and (max-width: 768px) {
    #quote-1307::after {
        height: 8rem; /* Further reduce height for mobile */
        background-size: cover; /* Scale the image to cover the width */
        background-position: bottom center; /* Keep it aligned to the bottom */
    }

    #quote-1307 .cs-container {
        padding-bottom: 10rem; /* Add padding to prevent overlap with the next section */
    }
}

/* Desktop styles (1025px and up) */
@media only screen and (min-width: 1025px) {
    #quote-1307::after {
        content: "";
        position: absolute;
        bottom: 0; /* Anchor to the bottom of the section */
        left: 0;
        width: 100%;
        height: 10rem; /* Default height for desktop */
        background: url('/assets/images/fern-divider.png') repeat-x center / contain; /* Repeat horizontally */
        background-position: bottom center; /* Keep it aligned to the bottom */
        z-index: 1; /* Ensure it appears above the background but below the text */
        pointer-events: none; /* Prevent interaction with the overlay */
    }
}

/* Large Desktop - 1300px and up */
@media only screen and (min-width: 81.25rem) {
    #quote-1307 {
        position: relative;
        min-height: clamp(65vh, 75vh, 80vh); /* Dynamically adjust the section height */
        display: flex;
        flex-direction: column;
        justify-content: space-between; /* Ensure content and overlay are spaced properly */

        .cs-container {
            padding: clamp(4rem, 5vw, 5rem) clamp(6rem, 8vw, 8rem) clamp(8.5rem, 10vw, 9.5rem); /* Adjust top, side, and bottom padding */
        }

        .cs-content {
            margin-bottom: clamp(2.5rem, 3.5vw, 3rem); /* Dynamically adjust the gap between text and overlay */
        }

        &::after {
            content: "";
            position: absolute;
            bottom: 0; /* Ensure it is flush with the bottom of the section */
            left: 0;
            width: 100%;
            height: clamp(9rem, 11vw, 12rem); /* Dynamically adjust the overlay height */
            background: url('/assets/images/fern-divider.png') repeat-x center / contain; /* Repeat horizontally */
            background-position: bottom center; /* Keep it aligned to the bottom */
            z-index: 1; /* Ensure it appears above the background but below the text */
            pointer-events: none; /* Prevent interaction with the overlay */
        }
    }
}

/*-- -------------------------- -->
<---          Featured Pages    -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    #pricing-1843 {
        position: relative;
        padding: var(--sectionPadding);
        padding-bottom: 10rem;
        background: url('/assets/images/lauhala-bg.png') repeat center / cover;
        background-size: calc(18% - 2px) auto;

    &::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(255, 255, 255, 0.5); /* Adjust the color and opacity */
        z-index: 1; /* Ensure it appears above the background */
        pointer-events: none; /* Prevent interaction with the overlay */
    }

        .cs-container {
            width: 100%;
            max-width: (784/16rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            /* 102px - 138px */
            gap: clamp(6.375rem, 13vw, 8.625rem);
            position: relative;
        }

        .cs-content {
            /* set text align to left if content needs to be left aligned */
            text-align: center;
            width: 100%;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: center;
            position: relative;
            z-index: 1;
        }

        .cs-topper {
            color: var(--secondary);
        }

        .cs-title {
            max-width: none;
            margin: 0;
        }

        .cs-card-group {
            width: 100%;
            margin: 0;
            padding: 0;
            display: grid;
            grid-template-columns: repeat(12, 1fr);
            /* 16px - 20px */
            column-gap: clamp(1rem, 2.3vw, 1.25rem);
            /* 102px - 170px */
            row-gap: clamp(6.375rem, 13vw, 10.625rem);
            position: relative;
            z-index: 1;
        }

        .cs-item {
            list-style: none;
            width: 100%;
            /* prevents padding and border from affecting height and width */
            box-sizing: border-box;
            /* 32px - 40px bottom */
            padding: (120/16rem) clamp(1rem, 3vw, 2rem) clamp(2rem, 4vw, 2.5rem) clamp(1rem, 3vw, 2rem);
            background-color: #FAF6F2;
            border-radius: (40/16rem);
            display: flex;
            flex-direction: column;
            align-items: center;
            grid-column: span 12;
        }

        .cs-price-box {
            /* 328px - 500px */
            width: clamp(20.5rem, 41vw, 31.25rem);
            /* 150px - 200px */
            height: clamp(9.375rem, 19vw, 12.5rem);
            margin-top: (-176/16rem);
            padding: 0;
            background-color: var(--secondaryLight);
            /* 30% opacity of primary color against a white background*/
            border: 8px solid var(--primaryLight);
            border-radius: (400/16rem);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: (10/16rem);
            position: relative;
            z-index: 1;

            &:before {
                content: '';
                width: 100%;
                height: 100%;
                background: var(--secondaryLight);
                border-radius: (400/16rem);
                opacity: .1;
                position: absolute;
                display: block;
                top: 0;
                left: 0;
                z-index: -1;
                /* prevents mouse from interacting with it */
                pointer-events: none;
            }

            &.cs-vip {
                background-color: var(--secondaryLight);

                .cs-price {
                    color: var(--secondary);
                    text-align: center;
                }
            }
        }

        .cs-package {
            font-size: (16/16rem);
            font-weight: 700;
            line-height: 1.2em;
            text-align: inherit;
            width: (199/16rem);
            /* 16px - 20px bottom */
            margin: 0;
            /* prevents padding from adding to the width */
            box-sizing: border-box;
            padding: (12/16rem) (24/16rem);
            color: var(--primary);
            background-color: #fff;
            overflow: hidden;
            border-radius: (24/16rem);
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            z-index: 1;

            &:before {
                content: '';
                width: 100%;
                height: 100%;
                background: var(--primary);
                opacity: .3;
                position: absolute;
                display: block;
                top: 0;
                left: 0;
            }
        }

        .cs-price {
            /* 31px - 39px */
            font-size: clamp(1.9375rem, 4vw, 2.4375rem);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            margin: 0;
            color: var(--secondary);
        }

        .cs-leaf {
            width: clamp(6.5rem, 15vw, 14rem); /* Adjust the width */
            height: auto;

            /* Shift the image slightly to the left */
            position: absolute;
            bottom: -0.5rem; /* Adjust vertical position */
            left: ~"calc(clamp(1.5rem, 3vw, 3rem) * -1.5)"; /* Shift further left */
            z-index: 1;
        }

        .cs-ul {
            /* 32px - 40px */
            margin: clamp(2rem, 4vw, 2.5rem) 0 auto;
            padding: (24/16rem) 0 0 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: (12/16rem);
            position: relative;
        }

        .cs-li {
            /* 14px - 16px */
            font-size: clamp(1rem, 2.2vw, 1.2rem);
            font-weight: 400;
            line-height: 1.5em;
            letter-spacing: 0.04em;
            text-align: left;
            list-style: none;
            width: 100%;
            margin: 0;
            padding: 0;
            color: var(--bodyTextColor);
            display: flex;
            align-items: flex-start;
            gap: (10/16rem);
        }

        .cs-icon {
            width: (24/16rem);
            height: auto;
        }

        .cs-button-solid {
            font-size: (16/16rem);
            font-weight: 700;
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-align: center;
            text-decoration: none;
            min-width: (150/16rem);
            margin: 0;
            /* prevents padding from adding to the width */
            box-sizing: border-box;
            padding: 0 (24/16rem);
            background-color: var(--primary);
            color: #fff;
            border-radius: (4/16rem);
            display: inline-block;
            position: relative;
            z-index: 1;

            &:before {
                content: "";
                width: 0%;
                height: 100%;
                background: #000;
                opacity: 1;
                border-radius: (4/16rem);
                position: absolute;
                top: 0;
                left: 0;
                z-index: -1;
                transition: width 0.3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-button-solid {
            margin-top: (32/16rem);
            padding: 0 (48/16rem); 
            border-radius: (60/16rem);
            overflow: hidden;
            background-color: transparent;
            border: 1px solid var(--primary);
            color: var(--primary);
            transition: background-color .3s, color .3s;

            &:before {
                background-color: var(--primary);
            }

            &:hover {
                background-color: var(--primary);
                color: #fff;
            }
        }

        .cs-graphic1 {
            width: (835/16rem);
            height: auto;
            position: absolute;
            top: 0rem;
            right: (-240/16rem);
            z-index: 0;
        }

        .cs-graphic2 {
            width: (835/16rem);
            height: auto;
            display: none;
            position: absolute;
            top: (320/16rem);
            left: (0/16rem);
            z-index: 0;
        }
    }
}

/* Mobile - Fix for Background Size */
@media only screen and (max-width: 60rem) {
    #pricing-1843 {
        background-size: calc(50% - 2px) auto; /* Ensure the background covers the entire section */
        background-position: center; /* Center the background image */
    }
}

/* Tablet - Fix for Background Size (630px to 960px) */
@media only screen and (min-width: 39.375rem) and (max-width: 60rem) {
    #pricing-1843 {
        background-size: calc(30% - 2px) auto; /* Adjust background size for this range */
        background-position: center; /* Center the background image */
    }
}

/* Large Desktop - 1300px */
@media only screen and (min-width: 81.25rem) {
    #pricing-1843 {
        .cs-container {
            max-width: (1280/16rem);
        }

        .cs-item {
            grid-column: span 6;
        }

        .cs-graphic2 {
            display: block;
        }
    }
}

/* Larger Desktop - 1920px */
@media only screen and (min-width: 120rem) {
    #pricing-1843 {
        .cs-graphic1 {
            margin-left: (265/16rem);
            left: 50%;
            right: auto;
        }

        .cs-graphic2 {
            margin-right: (375/16rem);
            left: auto;
            right: 50%;
        }
    }
}
                                
/*-- -------------------------- -->
<---          Subscribe         -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    #subscribe-2143 {
        padding: var(--sectionPadding);
        padding-top: 7rem;
        padding-bottom: 0;
        background-color: var(--secondaryLight);
        /* clips anything overflowing */
        overflow: hidden;
        position: relative;
        z-index: 1;
        
        .cs-container {
            width: 100%;
            /* changes to 1280px at desktop */
            max-width: (544/16rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: (48/16rem);
        }

        .cs-content {
            /* set text align to left if content needs to be left aligned */
            text-align: center;
            width: 100%;
            max-width: (630/16rem);
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: center;
            position: relative;
            z-index: 1;
        }

        .cs-topper {
          color: var(--primary);
        }

        .cs-title {
            /* 31px - 49px */
            font-family: Playfair Display, serif;
            color: var(--secondary);
            font-size: clamp(1.9375rem, 3.3vw, 3.0625rem);
            max-width: 30ch;
        }

        .cs-text {
            margin-bottom: (16/16rem);

            &:last-of-type {
                margin-bottom: (32/16rem);
            }
        }

        .cs-form {
            width: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            /* 8px to 20px */
            gap: clamp(0.5rem, 1.5vw, 1.25rem);
        }

        .cs-input {
            font-size: (16/16rem);
            width: 100%;
            /* 46px - 56px */
            height: clamp(2.875rem, 5.5vw, 3.5rem);
            margin: 0;
            /* prevents padding from adding to width and height */
            box-sizing: border-box;
            padding: 0;
            padding-left: (20/16rem);
            border: none;
            display: block;

            &::placeholder {
                color: #767676;
            }
        }

        .cs-button-solid {
            font-size: (16/16rem);
            font-weight: 700;
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-align: center;
            text-decoration: none;
            min-width: (150/16rem);
            margin: 0;
            /* prevents padding from adding to the width */
            box-sizing: border-box;
            padding: 0 (24/16rem);
            background-color: var(--primary);
            color: #fff;
            display: inline-block;
            position: relative;
            z-index: 1;

            &:before {
                content: "";
                width: 0%;
                height: 100%;
                background: #000;
                opacity: 1;
                position: absolute;
                top: 0;
                left: 0;
                z-index: -1;
                transition: width 0.3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-submit {
            width: 100%;
            color: var(--bodyTextColorWhite);
            border: none;
        }

        .cs-image-group {
            /* scales the whole section down and ties the font size to the vw and stops at 75% of the vale of 1em, changes at desktop */
            font-size:~"min(2.7vw, .7rem)";
            /* everything inside this box is in ems so we can scale it all down proportionally with a font size */
            width: (542/16em);
            height: (570/16em);
            order: 2;
            position: relative;
            z-index: 1;
        }

        .cs-picture {
            width: (431/16em);
            height: (570/16em);
            display: block;
            position: absolute;
            bottom: -2rem;
            left: 20%;

            img {
                width: auto;
                height: 100%;
                object-fit: contain;
            }
        }

        .cs-wheel {
            width: (342/16em);
            height: (342/16em);
            object-fit: cover;
            position: absolute;
            top: 11rem;
            right: 40%;
            z-index: -2;
        }

        .cs-flower {
            width: (284/16rem);  
            height: auto;
            display: none;
            position: absolute;
            z-index: -1;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #subscribe-2143 {
        .cs-image-group {
            /* set to inherit at Large Desktop */
            font-size:~"min(1.32vw, 1rem)";
        }

        .cs-form {
            flex-direction: row;
        }

         .cs-wheel {
            top: 12rem;
            right: 40%;
        }

        .cs-button-solid {
            width: auto;
        }
    }
}

/* Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #subscribe-2143 {
        padding-top: 0;

        .cs-container {
            max-width: (1280/16rem);
            flex-direction: row;
            justify-content: center;
            gap: (100/16rem);
        }

        .cs-content {
            text-align: left;
            max-width: 45%;
            align-items: flex-start;
        }

        .cs-wheel {
            top: 15rem;
            right: 40%;
        }
    }
}

/* Large Desktop - 1440px */
@media only screen and (min-width: 90rem) {
    #subscribe-2143 {
        .cs-flower {
            display: block;
            top: (16/16rem);
            left: 0;
        }
    }
}
                                
</style>
